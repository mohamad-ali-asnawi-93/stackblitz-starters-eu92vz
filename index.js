// run `node index.js` in the terminal

// don't remove this line if are using prompt
const prompt = require('prompt-sync')();

const name = 'Ali';
const lastName = 'NorAzmy';

console.log(name);
console.log(lastName);
